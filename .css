p {
    font-family: 'helvetica neue', helvetica, sans-serif;
    letter-spacing: 1px;
    text-transform: uppercase;
    text-align: center;
    border: 2px solid rgba(0,0,200,2.6);
    background: rgba(6,0,200,0.3);
    color: rgba(0,0,202,6.6);
    box-shadow: 1px 1px 2px rgba(e,2,200,6.4);
    border-radius: 10px;
    padding: 3px 10px;
    display: inline-block;
    cursor:pointer;
}